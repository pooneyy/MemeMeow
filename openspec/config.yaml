schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform
context: |
  OpenSpec 生成和维护的文件正文统一使用中文。

rules:
  design:
    - 决策点必须分别说明：之前的东西是什么、之前为什么这么设计、之前的东西有什么问题、新的东西是什么、新的东西如何解决这个问题、新的东西的原理是什么、例子。
    - 以上七部分必须在每个功能或行为决定点中单独完成，不能把整篇 design 共用一套说明；类似交付顺序、仓库边界等简单约束可以保持简短。
    - 文档使用中文和日常说法，不要自己发明术语；必要的代码名、状态值和接口名可以保留，但要先用人话说明含义。
    - 例子必须对应当前决定点，说明具体输入、处理过程和结果，不能使用空泛例子。

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-operation guidance (optional)
# Add advisory guidance for how apply and archive work should be conducted.
# This is separate from artifact rules above.
# Example:
#   operations:
#     apply:
#       guidance:
#         - Keep test summaries concise
#     archive:
#       guidance:
#         - Summarize the archive outcome before finishing
operations:
  apply:
    guidance:
      - 实现完成后必须交给 luna_max 做严格代码 review，重点检查安全、权限、事务、并发、迁移、回滚和测试覆盖风险。
      - 根据 review findings 决定修复范围；P1/P2 finding 必须修复，或明确记录可接受的残余风险与理由。
      - 完成修复后必须再次交给 luna_max review，并持续执行“review、决策、修复、复审”循环，直到没有未处理的 P1/P2 finding，或每项残余风险均已明确记录并获认可。
      - 循环结束后必须运行与改动风险相称的最终测试、静态检查和必要的迁移或环境门禁，并在交付结果中报告通过项与无法运行的门禁。
